name: tracker_testing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.7.0
  firebase_analytics: ^12.3.0
  flutter_bloc: ^9.1.1
  mixpanel_flutter: ^2.6.2
  get_it: ^9.2.1
  firebase_crashlytics: ^5.2.0
  amplitude_flutter: ^4.5.0
  amplitude_session_replay: ^0.1.0-alpha.14
  posthog_flutter: ^5.24.0
  mixpanel_flutter_session_replay: 0.1.0-beta.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
